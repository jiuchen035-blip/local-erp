# 账管卫士（LocalERP）开发踩坑全记录与技术要点

> 项目：进销存 + 财务记账 + AI 智能体一体化本地软件（Spring Boot 3 + Vue 3 + SQLite + Python 桥接）
> 版本：0.1.0 → 0.4.0（五次迭代）｜本文档记录开发全程的技术要点、踩过的所有坑及解决办法、需求迭代史。
> 用途：技术博客素材 / 面试深挖准备 / 后续接手开发的说明书。

---

## 一、项目概览与技术栈

| 层 | 技术 | 职责 |
|---|---|---|
| 后端 | Java 17 + Spring Boot 3.3 + MyBatis-Plus + SQLite | 业务建模、AI 应用层、备份恢复、REST API |
| 前端 | Vue 3 + Element Plus + ECharts + SheetJS + axios | 管理界面、导入向导、AI 聊天 |
| AI 应用层（Java） | OpenAI 兼容协议自封装（无 SDK） | Function Calling Agent 循环、三来源路由、RAG、Text2SQL |
| 网页桥接（Python） | FastAPI + Playwright + Scrapling | 把无 API 的网页版大模型封装成 OpenAI 兼容接口 |
| 打包分发 | jpackage + Inno Setup 6 + PowerShell | 免安装绿色版 + 单文件安装包，自动拉起 Python 桥接 |

数据/服务拓扑：`主程序(8080) ⇄ Python 桥接(127.0.0.1:8317) ⇄ Playwright 无头浏览器 ⇄ 网页版大模型`；云端 API / 本地 Ollama / 网页桥接三来源按请求自动降级。

---

## 二、技术要点

### 2.1 进销存 / 财务核心（Java）

- **库存 = 流水聚合**：库存数不落商品表，由 `stock_record` 流水按商品求和实时得出；流水唯一入口是单据过账（调拨生成一出一入两条），天然可追溯、可冲正
- **移动加权平均成本**：每次采购过账重算 `avg_cost`，销售流水结转成本，毛利 = 销售额 − Σ结转成本（调拨/报损/盘盈不计入经营流水）
- **冲正**：生成镜像反向单据并过账，原单保留；库存不足拒绝冲正；挂账单冲正自动结清应收应付
- **价税分离**：含税价 ÷ (1+税率) 拆净额与税额，按商品税率分组生成凭证分录，做成全局开关
- **幂等迁移**：所有 schema 变更走 `try { ALTER TABLE ... } catch (ignored) {}` + 预置数据幂等补齐，老库启动即自动升级，五个版本用户数据零丢失
- **热备**：启动 + 每晚 23 点 + 手动三个时机 `VACUUM INTO`，保留 30 份

### 2.2 AI 应用层（Java，自研 Agent 循环）

- **Agent 循环**：`while(轮次<8) { chatRaw(messages, tools) → 有 tool_calls 则 dispatch 执行 → 结果以 role:tool 拼回 }`，百行级实现，不依赖 LangChain
- **人机协同安全边界**：AI 只能 `create_draft_bill` 生成草稿，过账必须人工确认——幻觉最多到草稿为止
- **工具集**：商品检索/库存预警/仓库/往来单位/自动建档（销售→客户、采购→供应商）/创建草稿单/query_data（只读 SQL，SELECT 白名单+关键词黑名单+200 行上限+期初库存走 ADJUST 盘整流水不污染毛利）
- **三来源自动降级路由**：单请求级解析端点（有效 Key→云端 API → Ollama 在线→本地 → 否则拉起桥接走网页），不污染全局配置；探测带 30s 缓存；前端实时显示当前来源与降级原因
- **RAG**：中文 2-gram 关键词检索（零配置）+ 语义向量（余弦暴力检索）双模式，答案附来源，检索不到明说不知道
- **Text2SQL**：强制单条 SELECT + 关键词黑名单（INSERT/UPDATE/DROP/ATTACH/PRAGMA…）

### 2.3 网页大模型桥接服务（Python，核心创新点）

- **OpenAI 兼容封装**：FastAPI 暴露 `/v1/chat/completions`，Java 端把它当普通厂家直连，业务零改造
- **Function Calling 文本协议双向转换**：请求侧把 tools 定义渲染成文本协议注入 prompt；响应侧正则解析 `<tool_call>{JSON}</tool_call>`（容错尾逗号/无效 JSON）重组成标准 tool_calls——不支持 FC 的网页模型跑通多轮工具编排
- **每次请求开新会话**：强制回到站点首页导航，避免网页残留上下文污染多轮任务
- **登录态持久化**：Playwright `launch_persistent_context` 每站点独立 profile（可配置到数据目录，升级不丢）；登录窗口 = 有头浏览器 + 轮询检测
- **登录特征优先判定**：URL 跳到 `/sign_in`、`/miaoxiang/` 或登录按钮可见 → 一律未登录（登录页上也有别的输入框，"有输入框=已登录"会误报）
- **回答完成判定**：候选选择器轮询 + **静默 8 秒**（流式输出停顿 >3s 是常态，8s 才稳定）且总上限 420s（兼容深度思考）；选择器全失效时 Scrapling 解析全量 HTML 自适应兜底
- **整段粘贴式输入**：`locator.fill(prompt)` 一次性赋值——逐字打字会让每个换行符触发网页的"回车发送"（见坑 #3）
- **行为治理**：prompt 结构重排（系统设定→工具参考→用户请求置底）、负面指令（禁复述工具清单/禁联网搜索）、发送前自动关闭站点"深度思考/联网搜索"开关（能识别已开启才点）
- **健壮性**：导航 3 次重试 + commit 兜底、窗口持续空白自动关闭并明确报错、每站点 asyncio 串行锁、错误信息（未登录/站点忙/导航失败）透传到前端

### 2.4 前端（Vue 3 + Element Plus）

- 大列表**客户端分页**（每页 100 行）治 el-table 千行渲染卡顿；导出仍走全量筛选结果
- `el-select` 放进 `el-popover` 必须 `:teleported="false"`（见坑 #16）
- `el-upload` 的 change 回调参数是 UploadFile 对象，真实文件在 `.raw`（见坑 #14）
- 文件选择必须 `ref.click()` 直调，不能 label 包按钮（见坑 #15）
- 错误信息统一提取：后端 `error` 字段 / FastAPI `detail` 字段 / HTTP 状态码，三层兜底展示

### 2.5 打包分发

- jpackage app-image 自带精简 JRE（`--add-modules` 明确列出，减模块省几十 MB 但漏一个就运行期 NoClassDefFoundError）
- AppHome 数据目录三级定位：jpackage 属性 → **jar 旁 .cfg 文件探测**（最可靠）→ 工作目录；数据自包含，删目录即卸载
- Inno Setup `PrivilegesRequired=lowest` 按用户安装免 UAC；`.iss` 保存 UTF-8 无 BOM；`UninstallDelete` 连数据一起删
- PowerShell 一键流水线（前端构建→后端打包→jpackage→zip→ISCC），bat 只做 ASCII 薄壳（中文+括号路径会让 cmd 命令错乱）
- 桥接服务按需自动拉起（ProcessBuilder + 健康检查等待），Python 环境缺失时给出明确中文提示

---

## 三、踩坑全记录（现象 → 原因 → 解决 → 一句话经验）

### 3.1 AI 网页桥接类

**坑 #1：妙想适配器地址写错，登录窗口打不开聊天页**
- 现象：点"妙想登录"弹出的浏览器停在空白页（about:blank），无法登录
- 原因：适配器 URL 写成了 `miaoxiang.eastmoney.com`，真实聊天页是 `https://ai.eastmoney.com/chat`；且导航失败后无重试无报错，窗口停在空白页干等超时
- 解决：修正 URL；导航改 3 次重试 + 90s 超时 + `commit` 兜底；窗口持续空白 30 秒自动关闭并提示"页面未能打开，请检查网络"
- 经验：**驱动第三方网页，URL 和登录入口必须以真实浏览器截图为准，不能凭印象写**

**坑 #2：登录检测误报（"登录成功"其实没登录）**
- 现象：登录窗口提示"登录成功，登录态已保存"，实际发消息报未登录
- 原因：`is_logged_in` 用"页面上有可见输入框"判断，而登录页上也渲染了别的输入框
- 解决：**登录特征优先**——URL 含 `/sign_in`（DeepSeek）或 `/miaoxiang/`（妙想落地页）、登录按钮可见 → 一律未登录；输入框只是辅助条件
- 经验：**状态判定要让"否定特征"有一票否决权，而不是只看肯定特征**

**坑 #3：长提示词被拆成几十条碎片消息**
- 现象：网页会话里出现几十条一问一答，模型每段回一句"收到，已记录"，用户的问题淹没在里面
- 原因：用 Playwright `type()` 逐字打字输入，prompt 里的**每个换行符都触发了网页的"回车=发送"**，一条长消息被打成碎片逐条发出
- 解决：改 `locator.fill(prompt)` 整段粘贴式赋值（不产生逐字按键事件，换行只是文本）+ 单次回车 + 检查输入框残留自动补发
- 经验：**自动化输入要按"目标站点如何看待这次输入"设计，而不是按"我怎么构造字符串"设计**

**坑 #4：模型不干活，反而把工具清单背了一遍**
- 现象：问"可口可乐库存还剩多少"，模型回复"收到！query_data 工具已完整记录"并列出 7 个工具，问题没处理
- 原因：工具说明放在 prompt 最前面，网页模型把它理解成"教学确认"任务，复述清单交差
- 解决：prompt 重排——系统设定在前 → 工具参考标注"仅任务需要时使用，禁止复述" → **用户请求放最末尾** → 显式指令"现在直接处理上面最后一个用户请求"；系统提示词里也加"禁止罗列工具清单"
- 经验：**对网页模型，"最后一条消息"就是它的任务焦点，指令顺序即优先级**

**坑 #5：模型跑去做联网搜索，不到本地数据库查**
- 现象：问库存，模型回答"搜索结果中没有找到百事可乐"并展示"已显示 15 个网页"——它去互联网搜了
- 原因：DeepSeek 账号级"联网搜索"开关处于开启状态（跟随账号记忆），模型顺手用了
- 解决：发送前自动识别并关闭"深度思考/联网搜索"开关（class 判定已开启才点，避免把关着的点开）+ 指令明令"系统数据只在本地，互联网查不到，禁止联网搜索"
- 经验：**账号级持久开关是隐形变量，每次任务前应程序化归零；模型自发使用外部工具时要显式圈定数据边界**

**坑 #6：回答被拦腰截断**
- 现象：回答结尾出现"**可"这种半截话
- 原因："流式输出静默 3 秒 = 回答完成"的判定太激进，网页流式偶发停顿超过 3 秒就被误判为已结束
- 解决：静默阈值 3s → 8s，最小等待 2s → 3s，总超时 240s → 420s（兼容深度思考模式先"想"几分钟的场景）
- 经验：**流式系统的完成判定阈值要按"最坏网络停顿"而不是"平均情况"设置**

**坑 #7：Agent 多轮任务超时**
- 现象：开单任务 240 秒报"等待回答超时"
- 原因：网页模型处理长 prompt + 可能的深度思考，总时长超出预设上限
- 解决：总超时提到 420 秒；超时报错文案明确说明"站点可能改版或响应极慢"，并对网页模式设定合理预期（每轮 40~90 秒，多轮任务数分钟）
- 经验：**给异步系统设超时前，先实测 P99 耗时，并让超时报错本身携带诊断信息**

**坑 #8：Java 发的请求被桥接判成空 body（HTTP 422）**
- 现象：Java 调桥接必报 `{"detail":[{"loc":["body"],"msg":"Field required","input":null}]}`，curl 直连完全正常
- 原因：Spring RestClient 默认底层是 JDK HttpClient，POST 用 **Transfer-Encoding: chunked + h2c 升级头**、无 Content-Length；uvicorn 把这种请求当空 body。云端 API 服务器容忍这种格式，所以之前没暴露
- 解决：RestClient 换 `SimpleClientHttpRequestFactory`（HttpURLConnection：缓冲 body、显式 Content-Length、纯 HTTP/1.1）
- 经验：**"curl 正常、程序失败"就抓原始报文对比**——用回显服务器打印 method/headers/body，一次定位。跨语言联调时 HTTP 细节（分块、协议升级、编码）是重灾区

**坑 #9：第二次请求接在网页旧会话后面（上下文污染）**
- 现象：多轮对话时模型看到的上下文混乱，回答跑偏
- 原因：适配器只在 URL 不同才重新导航，第二次请求直接接在网页已有对话之后，网页侧残留上一轮内容
- 解决：每次请求都强制 goto 站点首页开新会话（登录态在 profile 不受影响）
- 经验：**"无状态请求"的语义要由调用方显式保证，不能依赖对端页面的当前状态**

**坑 #10：妙想登录窗口停在 about:blank 无提示挂死**
- 现象：登录窗口打开后一直白屏，5 分钟后超时
- 原因：`goto` 导航失败（网络/反爬）不重试不报错
- 解决：导航 3 次重试（最后一次放宽到 commit 事件）；轮询中检测 `page.url == about:blank` 持续 30 秒 → 自动关窗并报"页面未能打开，请检查网络"
- 经验：**凡是可能静默失败的自动化步骤，都要有"观察-超时-明确报错"三件套**

### 3.2 Java / Spring / HTTP 类

**坑 #11：中文路径下 `mvn spring-boot:run` 报 ClassNotFound**
- 现象：项目路径含中文和空格时，spring-boot-maven-plugin 启动报 `ClassNotFoundException`
- 原因：插件生成的 JVM 参数文件在中文 Windows 下编码错乱
- 解决：统一 `java -jar target\xxx.jar` 运行
- 经验：**中文 Windows + 中文路径是 Java 工具链的重灾区，能绕就绕**

**坑 #12：Java 端拿不到上游真实报错**
- 现象：前端只显示"测试失败"，不知道原因
- 原因：异常文案被通用处理器吞掉，FastAPI 的 `detail` 字段和后端 `error` 字段形态不同
- 解决：桥接层错误全部带中文语义（未登录/站点忙/导航失败）；Java 透传响应体；前端 `errMsg` 按 error → detail → HTTP 状态码三层提取
- 经验：**错误链路每一跳都要保留语义，报错文案本身就是排障效率**

**坑 #13：Double 装箱类强转 int 编译错**
- 现象：`(int) dblVal(...)` 编译失败"Double 无法转换为 int"
- 解决：加 `intVal` 辅助方法（`(int)(double)` 两步拆箱）
- 经验：**包装类不能直接强制转换成另一个基本类型，先拆箱再窄化**

### 3.3 前端类

**坑 #14：商品图片上传"没反应"**
- 现象：选了图片，毫无反应
- 原因：Element Plus `el-upload` 的 change 回调参数是 **UploadFile 对象**（真实文件在 `.raw`），旧代码按原生事件读 `e.target.files` 读到 undefined，静默失败
- 解决：`const file = uploadFile?.raw || uploadFile?.target?.files?.[0]` 双形态兼容
- 经验：**组件库回调参数 ≠ 原生 DOM 事件，升级或混用时先确认回调签名**

**坑 #15：label 包按钮 + 隐藏 input，点击没反应**
- 现象：批量导入/备份上传点"选择文件"按钮，文件框弹不出来
- 原因：`<label>` 里套的是 Element Plus 按钮（渲染为原生 `<button>`）——HTML 规范规定点击目标是交互元素时 label 不转发点击
- 解决：`ref` 引用 input，按钮 `@click="inputRef?.click()"` 直调
- 经验：**隐藏 file input 的标准唤醒方式就是 `.click()`，label 方案别和组件库按钮混用**

**坑 #16：el-popover 里点 el-select 选项，整个弹层关闭**
- 现象：开单选品弹层里选一级分类，选完弹层没了
- 原因：el-select 下拉选项默认 **teleport 到 body** 渲染，点选项 = 点击 popover 外部 → trigger=click 的 popover 自闭
- 解决：三个 select 加 `:teleported="false"`，下拉渲染在弹层内部
- 经验：**弹层组件套弹层组件，先查 teleport 行为**

**坑 #17：商品列表 1440 行渲染卡顿**
- 现象：分类浏览切来切去明显卡顿
- 原因：el-table 全量渲染上千行（每行还有图片/上传组件），每次切分类重渲染
- 解决：客户端分页每页 100 行 + 切换筛选自动回第 1 页；导出/统计仍用全量筛选结果
- 经验：**表格组件的渲染上限远低于数据上限，"数据能算出来"和"能渲染出来"是两回事**

### 3.4 打包分发类

**坑 #18：另一台电脑 "Failed to launch JVM"**
- 现象：安装/解压后双击 exe 报 JVM 启动失败
- 原因（按概率）：①绿色版没解压直接在压缩软件里双击 exe（Windows 只解出 exe 单文件，找不到自带 JRE）；②Win7/8/32 位系统（内置 JRE 需 Win10+ x64）；③杀软隔离 runtime 文件
- 解决：README 排查清单（exe 旁必须有 app/ 和 runtime/ 目录）+ 包内附 **控制台启动.bat**（绕过 jpackage 启动器直接 `runtime\bin\java -jar`，既绕开启动器问题又能把真实报错显示出来）
- 经验：**给非技术用户分发软件，要为"必然出现的错误路径"准备一条兜底启动方式和看得见的报错**

**坑 #19：双击 exe 后数据写到奇怪的位置**
- 原因：双击启动时工作目录不可控（快捷方式/任务栏/运行对话框各不同），相对路径落盘错乱
- 解决：AppHome 三级定位（jpackage 注入属性 → jar 旁 `.cfg` 文件探测（打包态最可靠标志）→ 工作目录），所有落盘路径跟随
- 经验：**桌面应用第一行代码就该解决"我的数据放哪"**

**坑 #20：jpackage 图标静默不生效**
- 原因：只有 PNG 条目的 ICO 被 jpackage 静默忽略（不报错，图标还是默认杯子）
- 解决：脚本生成 16~256 全套 **BMP 条目** ICO；用 ExtractAssociatedIcon 验证（注意 PNG 条目它也不认）
- 经验：**jpackage 对资源格式的校验是"静默忽略"型，产物必须逐项目视检**

**坑 #21：中文+括号路径下 bat 打包脚本命令错乱**
- 现象：bat 里报"'呭寘' 不是内部或外部命令"
- 原因：cmd 的代码页切换和括号块解析在中文路径下错乱
- 解决：核心逻辑全部放 PowerShell（原生 Unicode），bat 只做一行 ASCII 薄壳调 `powershell -File`
- 经验：**Windows 中文环境下，cmd 只当启动器，逻辑全放 PowerShell**

### 3.5 工具链类

**坑 #22：Git Bash curl 发中文 body，后端报 UTF-8 解析错误**
- 现象：测试接口时"JSON parse error: Invalid UTF-8 middle byte"，实际功能是好的
- 原因：Git Bash 控制台按 GBK 编码命令行里的中文，curl 原样发出
- 解决：中文 body 写入 UTF-8 文件后 `curl --data-binary @file`
- 经验：**Windows 下命令行直接内联中文测试参数不可信，用文件传**

**坑 #23：打包时输出目录删不掉（被进程占用）**
- 现象：`Remove-Item out\账管卫士` 报"正由另一进程使用"
- 原因：①残留 java/python 进程；②**shell 自身的工作目录停在该文件夹里**（占用目录句柄）
- 解决：先杀进程、**把 shell 的 cd 挪走**再删；实在不行等几秒重试
- 经验：**Windows 目录句柄被占，最先检查"是不是自己的 shell 站在里面"**

**坑 #24：pip 安装 scrapling 报 ResolutionImpossible**
- 解决：重跑一次安装成功（版本解析顺序问题）；关键包（fastapi/playwright/scrapling）装完后逐个 import 验证
- 经验：**依赖冲突先重试再排查，安装完用 import 自检**

---

## 四、需求迭代全记录（按提出顺序，全部已解决）

| # | 需求 / 反馈 | 解决方式 | 状态 |
|---|---|---|---|
| 1 | AI 聊天接入网页版模型（妙想+DeepSeek），支持自动降级，Java 自动拉起 | `webllm/` Python 桥接 + AiRouter 三来源路由 + Java 自动拉起/登录管理 | ✅ |
| 2 | 打包 exe；README 内容对不上；别电脑报 Failed to launch JVM | 0.4.0 双产物 + 备用控制台启动器 + README 重写 + FAQ 排查清单 | ✅ |
| 3 | 妙想地址不对；DS 登录打开失败；测试连接失败 | URL 修正、登录特征优先判定、错误信息透传 | ✅ |
| 4 | 能回答了但解决不了实际问题、不能代办 | 「自由问答」升级为带全部工具的智能助手（含只读 SQL 工具），草稿在聊天页确认 | ✅ |
| 5 | AI 开单报 Field required；妙想登录 about:blank | RestClient 工厂修复空 body；导航重试+空白检测 | ✅ |
| 6 | 模型复述工具清单不干活；回答被截断 | prompt 重排（请求置底+禁复述）；静默判定 3s→8s | ✅ |
| 7 | 为什么提示词一段一段给 | 整段粘贴式输入修复打字机拆分 | ✅ |
| 8 | 还是不成功（联网搜索跑偏） | 自动关联网搜索/深度思考开关 + 指令禁令 | ✅ |
| 9 | 商品管理要三级分类、默认分类浏览（留开关）；开单选品也要分类式；AI 开单确认按钮应在聊天页 | 三级分类字段+迁移、分类浏览+磁贴下钻、CategoryProductPicker 组件、chat 接口返回 draft 聊天页渲染确认卡 | ✅ |
| 10 | 商品图片上传没反应；知识库格式太少；选品要三级下拉+名称搜索 | el-upload raw 修复；POI/PDFBox 后端解析 docx/pdf/xlsx；选品组件重构为三级级联下拉+关键词 | ✅ |
| 11 | SKU 能不能自动生成（XF-010001 规则） | next-sku 接口（拼音缩写+分类排序编号+最大序号），前端自动预填、手改不覆盖 | ✅ |
| 12 | 批量导入商品；AI 图片导入；备份与数据导入恢复；异构 ERP 格式怎么解决 | 两阶段智能列映射导入（AI 图片导入按需求暂缓）；备份恢复=挂起文件+重启生效+上传/下载 | ✅ |
| 13 | 误输入的分类删不掉 | 分类管理（重命名/删除批量同步到商品） | ✅ |
| 14 | 导出表没有分类列怎么办 | 映射可留空 + 本批统一指定分类 + 未分类兜底 + SP 前缀 | ✅ |
| 15 | 直接帮我安装好 | 静默安装 + 启动验证 | ✅ |
| 16 | 批量导入点选择文件没反应 | label+button 转发失效 → ref.click() 直调 | ✅ |
| 17 | 表和系统列名一样但只有一个分类选项 | 映射目标补二级/三级分类独立列 | ✅ |
| 18 | 分类浏览很卡；选品点一级分类弹层就关 | 客户端分页每页 100 行；el-select teleported=false | ✅ |

---

## 五、方法论沉淀

1. **最小复现区分"谁的锅"**：跨系统联调出问题，先构造最小链路隔离变量（回显服务器对比原始报文），不要在集成环境里盲试
2. **网页模型 = 每次都是新员工**：任务、约束、输出格式在每次对话里一次性说清；能程序化锁死的（开关、导航、会话隔离）绝不靠模型自觉
3. **状态判定给否定特征一票否决权**：登录判断、完成判断这类布尔状态，"坏特征可见即坏"比"好特征可见即好"稳得多
4. **自动化输入/操作按目标系统视角设计**：它如何解释你的输入（换行=发送？按钮吞点击？）比你怎么构造更关键
5. **错误链路保留语义**：每一跳透传原始错误，报错文案就是排障效率
6. **破坏性操作前自动保险**：恢复前自动备份当前库、冲正不删原单、覆盖安装不碰数据目录
7. **给非技术用户分发软件**：为必然出现的错误路径准备兜底启动方式和看得见的报错
