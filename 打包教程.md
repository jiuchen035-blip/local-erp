# 账管卫士（LocalERP）打包分发教程

> 结论先行：**可以打包，而且两种方式都已经做好并实测通过。**
> 产物在 `local-erp/out/` 目录：
>
> | 产物 | 大小 | 给对方后对方怎么用 |
> |---|---|---|
> | `账管卫士-绿色版-0.4.0.zip` | 约 76 MB | 解压 → 双击 `账管卫士.exe` → 自动弹浏览器 → 直接用；**删掉文件夹 = 完全卸载** |
> | `账管卫士-Setup-0.4.0.exe` | 约 65 MB | 双击安装 → 桌面出现快捷方式；控制面板"应用"里可卸载，卸载即彻底清除 |
>
> 两种包都**自带精简版 Java 运行时**，对方电脑**不需要安装任何东西**（不用装 JDK、不用装数据库）。

---

## 一、日常打包：双击两个 bat 就够了

所有打包动作都在 `local-erp/package/` 目录里：

| 想要什么 | 双击哪个 | 产物 |
|---|---|---|
| 免安装绿色版 | `build-portable.bat` | `out/账管卫士-绿色版-0.4.0.zip` |
| 单文件安装包 | `build-installer.bat` | `out/账管卫士-Setup-0.4.0.exe` |

说明：
- `build-installer.bat` 发现没打过绿色版时会**自动先跑一遍**绿色版流程，所以直接双击它一个也能出安装包。
- 脚本核心逻辑在 `build.ps1`（PowerShell，中文路径下比 bat 稳），两个 bat 只是双击入口。
- 打包前**先退出正在运行的 账管卫士.exe**，否则删除旧文件夹时会被占用导致失败。

## 二、一键打包到底做了什么（4 步）

```
npm run build          ← 前端 Vue 打包成静态文件
mvn package            ← 后端打成本地 ERP 的 jar，前端页面自动打进 jar 的 static/
jpackage               ← JDK 官方工具：jar + 精简 JRE + 图标 → 账管卫士.exe 程序文件夹
Compress-Archive       ← 文件夹压成 zip（绿色版产物）
ISCC.exe               ← Inno Setup 把文件夹再压成单文件安装包（安装版产物）
```

对应脚本里的关键命令（想手动执行也可以，在 `local-erp` 目录下）：

```bat
:: 1. 前端
cd frontend && npm run build && cd ..

:: 2. 后端 jar（前端会自动打进去）
cd backend
set JAVA_HOME=C:\Users\ZhuanZ\erp-tools\jdk-17.0.20.1+1
C:\Users\ZhuanZ\erp-tools\apache-maven-3.9.16\bin\mvn.cmd -DskipTests package
cd ..

:: 3. 绿色版程序文件夹（自带 JRE）
"C:\Users\ZhuanZ\erp-tools\jdk-17.0.20.1+1\bin\jpackage.exe" ^
  --type app-image --name 账管卫士 --app-version 0.4.0 --vendor local-erp ^
  --icon package/app-icon.ico --input package/staging --main-jar local-erp-0.4.0.jar ^
  --add-modules java.se,jdk.unsupported,jdk.crypto.ec,jdk.crypto.cryptoki,jdk.zipfs,jdk.charsets,jdk.localedata ^
  --java-options "-Dfile.encoding=UTF-8" --java-options "-Xmx768m" --dest out
:: （package/staging 里放的就是 backend/target/local-erp-0.4.0.jar）

:: 4. 压缩 zip（绿色版完成）
powershell -Command "Compress-Archive -Path out\账管卫士 -DestinationPath out\账管卫士-绿色版-0.4.0.zip -Force"

:: 5. 安装包（需要 Inno Setup 6，本机已装好）
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" /DMyAppVersion=0.4.0 package\LocalERP-Setup.iss
```

## 三、★ 改了代码之后，一键启动的是哪个版本？（重点）

先记住一句话：**exe 里装的是"打包那一刻"的代码快照，不是你的源码。**

| 启动方式 | 跑的是哪份代码 | 改代码后要做什么 |
|---|---|---|
| 双击 `out/账管卫士/账管卫士.exe`（或别人电脑上的） | 打包时打进去的 jar | **重新打包**（双击 build 脚本），exe 才变成新版 |
| 双击项目根目录的 `start-backend.bat`（自己开发用） | `backend/target/` 下的 jar | 改完代码先 `mvn package` 再 start-backend.bat |
| 双击 `start-frontend.bat`（前端热更新开发） | `frontend/src` 源码实时生效 | 刷新浏览器即可，不用打包 |

所以给客户/朋友更新功能的完整流程是：

```
改代码 → 双击 build-portable.bat（或 build-installer.bat）→ 把新 zip / 新 Setup 发给对方
```

**升级不会丢数据**：
- 绿色版：让对方把新的 `账管卫士` 文件夹解压覆盖旧文件夹前，先把旧文件夹里的 `app/data` 拷出来，覆盖后放回去；或者干脆只把新的 `app/local-erp-0.4.0.jar` 发过去让他替换旧 jar。
- 安装版：版本号没变也会覆盖安装（AppId 固定），安装程序只覆盖程序文件，`app/data` 里的数据自动保留。

## 四、本机打包环境（换电脑时按此准备）

| 工具 | 用途 | 本机位置 / 获取方式 |
|---|---|---|
| JDK 17+（含 jpackage/jlink） | 编译 + 生成自带 JRE | 已有：`C:\Users\ZhuanZ\erp-tools\jdk-17.0.20.1+1`；换机装 Temurin 17 即可 |
| Maven 3.9 | 后端构建 | 已有：`C:\Users\ZhuanZ\erp-tools\apache-maven-3.9.16` |
| Node.js + npm | 前端构建 | 已装（v24） |
| Inno Setup 6（免费开源） | 单文件安装包 | 已装：`C:\Program Files (x86)\Inno Setup 6`；新机去 https://jrsoftware.org/isdl.php 下载（6.5 以上），装完无需任何配置 |

路径变了就改 `package/build.ps1` 顶部的"配置区"（JDK、Maven 路径，产品名，版本号都在那几行）。

## 五、对方拿到手之后

**绿色版**：
1. 解压 zip，得到 `账管卫士` 文件夹（放哪都行，桌面/D 盘都可以）
2. 双击里面的 `账管卫士.exe`
3. 几秒后浏览器自动打开 `http://localhost:8080`，登录 `admin / admin123`
4. 不想要了：退出程序，删掉整个文件夹即可，注册表、系统目录都不会有残留

**安装版**：
1. 双击 `账管卫士-Setup-0.4.0.exe` → 一路下一步（免管理员权限，不会弹 UAC 蓝色确认框）
2. 完成后桌面出现"账管卫士"快捷方式，双击即用
3. 卸载：Windows 设置 → 应用 → 账管卫士 → 卸载，**连数据库、备份、日志一并删光**

## 六、常见问题

**1. 对方双击 exe 弹蓝色"Windows 已保护你的电脑"？**
因为 exe 没有花钱买代码签名证书，Windows 对无名软件都会提示。点"更多信息"→"仍要运行"即可。发自己熟人用没问题；要彻底消除提示需要买代码签名证书（每年几百到几千元不等），现阶段没必要。

**2. 杀毒软件报毒/误删？**
未签名 exe 的常见误报。让对方把文件加入白名单，或换用绿色版 zip。

**3. 8080 端口被占用？**
程序会自动依次尝试 8080 → 8081 → 8082 → 18080，都不行才报错。浏览器没自动弹时，看 `app/data/logs/local-erp.log` 最后一行"Started on port xxxx"，手动访问 `http://localhost:xxxx`。

**4. 想改名 / 换图标 / 改版本号？**
- 改名：`build.ps1` 配置区 `AppName`；浏览器标签标题在 `frontend/index.html` 的 `<title>`
- 换图标：换一张 ≥512×512 的方图，运行 `node package/make-icon-from-image.js 图片路径`（自动去水印/裁边/圆角，生成 `package/app-icon.ico`），再重新打包
- 版本号：`build.ps1` 配置区 + `package/LocalERP-Setup.iss` 里的默认版本（命令行编译时 `/DMyAppVersion=` 会覆盖它）

**5. 换电脑/重装系统，账目数据怎么迁走？**
所有数据都在一个地方：程序文件夹里的 `app/data/`（数据库、商品图片、AI 配置、日志）。把 `账管卫士` 整个文件夹拷到新电脑 = 程序 + 数据一起走；只拷 `app/data` 放进新装的程序文件夹 = 只迁数据。程序每晚 23 点还会自动备份到 `app/backup/`，保留 30 份。

**6. 给别人的电脑是 Mac / Linux 怎么办？**
本教程的 exe 只适用于 Windows。jpackage 本身支持跨平台打包，但要在对应系统上打（Mac 出 .dmg 需要在 Mac 上执行 jpackage）。

## 七、本次打包改动了哪些代码（为什么）

为了让"删文件夹=完全卸载"成立，数据必须固定存在**程序包自己的目录**里，而不是随启动位置乱跑：

- 新增 `backend/.../AppHome.java`：识别 jpackage 打包运行模式（jar 旁有 `.cfg` 标志）→ 数据根目录定为程序文件夹；开发态退回原行为（`backend/data`），老数据不受影响
- `ErpApplication.java`：启动第一行统一设置 `erp.data.root`
- `application.yml`、`AiSettingsService`、`ShopInfoService`、`ProductController`、`ImagesController`、`BackupService`：所有落盘路径改为跟随 `AppHome`
- `frontend/index.html`：标题改为"账管卫士"
- 新增 `package/`：`build.ps1`（核心脚本）、两个 bat 入口、`LocalERP-Setup.iss`（Inno 脚本）、`make-icon-from-image.js`（图标生成）、`app-icon.ico`

实测记录：绿色版 exe 从任意目录启动数据都落在包内 ✓；安装包静默安装→运行→静默卸载→目录连同数据全部清除 ✓。
